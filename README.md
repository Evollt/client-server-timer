# Клиент серверный таймер

## Как запустить

1. `git clone git@github.com:Evollt/client-server-timer.git`
2. `composer update` && `npm i`
3. `php artisan serve`
4. `cd vue` && `npm i`
5. `npm run dev`

### Далее открываем http://localhost:3000
